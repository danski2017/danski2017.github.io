---
type: platform_change_receipt
status: archived
created: 2026-07-16
tags: [atlas-platform, phase-4, a2-convention, receipt]
---

# 2026-07-16 Phase 4 Receipt — gamma-ref Convention Lane (A2 Tier)

## Change
- Bake + viewer gained the **scene-own physical-metric norm** variant (witness
  option "E_ij TF, gamma-own"): on the conformally flat slice
  ||A||_gamma,scene = psi^-4 ||A||_delta, so the DEN A parity log-ratio shifts
  by 4*log10(Pp_ablated/Pp_full), applied exactly in the march. Baked as
  A_TFG for every arrangement; live fallback implements the same shift;
  DEN A only (declared). Viewer prints an **A2 convention-survival ledger**:
  per-source median crossing shift gamma-own vs delta-chart.

## First A2 results (frozen certified march)
- **Gaia LSN-66 (weak field): d(gamma,delta) = 0.00% on every source** —
  parity loci SURVIVE the convention change; weak-field foam claims are
  norm-convention-invariant. (Coincident companions NaN: no cell in either
  convention, consistently.)
- **Family S (compact, phi<=0.3): loci do NOT coincide** — d(gamma,delta)
  ~ 14.9-15.7% for tight-pair members, 5.1% for the loner drift; bias
  NEGATIVE everywhere (gamma-own locus sits INSIDE the delta-chart locus).
  Compact-regime parity claims are convention-sensitive with a now-measured
  shift; per A2 doctrine the sensitivity is reported as part of the result,
  not hidden.

## Validation
node --check pass; frozen counts unchanged for delta lanes; ledger verified
in-browser both scenes; DEN forced to A under gamma-own; no JS errors.

## Boundary
gamma-own is ONE lawful reference-metric declaration (each tensor normed with
its own scene's physical metric). Other lawful conventions (common full-scene
gamma-ref, invariant eigenvalue comparison) remain future lanes. Eigen/network
layers remain declared delta-chart.

## Hashes
Backup backups/atlas_platform/index_pre_phase4_20260716.html; after: d9b8f27ab31bb27824ae829d71924be1eda64f3fc2c2a292a4d7ace36c76a361
