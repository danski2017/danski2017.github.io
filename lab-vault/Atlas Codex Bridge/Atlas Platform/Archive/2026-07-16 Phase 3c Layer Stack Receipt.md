---
type: platform_change_receipt
status: archived
created: 2026-07-16
tags: [atlas-platform, phase-3c, layer-stack, wink, receipt]
---

# 2026-07-16 Phase 3c Receipt — Unified Layer Stack + Ablation Wink

## Change
- **Layer Stack** (BL lane): eigen and relational-network layer sections now
  live under one provenance-badged group ("Layer Stack — Atlas Native · BL
  substrate", badge "native"), visually unified with a stack border; all
  existing element ids and handlers preserved (additive regroup only).
  New master control: "all layers off" button (unchecks + dispatches change
  on all seven layer toggles).
- **Ablation wink** (universal A/B scrubber grammar, blueprint directive 5):
  full / removed / wink buttons under the Configuration select — wink
  alternates the arrangement between FULL SCENE and the chosen ablation every
  1.4 s using the frozen payloads (instant swaps). Guarded no-op in the
  interior lab.
- **Composed-stack passport line**: the passport ribbon now lists the active
  layer composition (e.g. "stack foam + eigen-delta + dominance"), refreshed
  on every layer toggle.

## Validation (independent adversarial verifier, all PASS)
Syntax (node --check); page ready; stack structure contains both sections;
all-layers-off round-trip; passport stack line; wink full/removed/play cycle
observed toggling 2<->0 and halting on stop; interior guard (no motion, no
errors); four-lane regression (Atlas Native, Recovery 10,648 probes, Imported
6 workflows, BL frozen 1934 pts); zero window errors throughout.

## Rollback
backups/atlas_platform/index_pre_phase3c_20260716.html
After SHA-256: 558ef94f770deadd31c790f96167de4da3ec59f0eb3b7f8b32514e1b1c146c39
