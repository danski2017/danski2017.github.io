---
type: platform_change_receipt
status: archived
created: 2026-07-17
tags: [atlas-platform, wink, commons, scrubber, receipt]
---

# 2026-07-17 Receipt — Commons<->Network Wink (universal scrubber grammar)

## Change
`index.html` BL lane, Commons Layers section: three buttons
(commons / network / wink) implementing the blueprint's universal A/B
scrubber grammar over LAYER SETS. A-view = commons trio (webbing +
islands + frontier); B-view = network parcels (dominance cells +
attribution entropy); play alternates at 1.4 s. Implementation drives
the existing layer CHECKBOXES (checked + change event), so every render
pipeline, the passport declaration, and all-layers-off remain truthful
with zero new render paths. gaia66-gated (commons payload is
base-model-only); buttons no-op elsewhere.

## Validation (in-browser)
- commons view -> passport "stack foam + commons-webbing +
  commons-islands + commons-frontier"; network view -> "stack foam +
  dominance + entropy".
- play alternates the two stacks; second press stops cleanly in the
  current phase; familyS click no-ops ("stack foam"); all-layers-off
  resets. Zero console errors throughout.

## Boundary
Pure UI composition over frozen payloads; no numerical claims changed.

## Rollback
Remove the blCw* button row and the cwSet/cwStop wiring block from
index.html (session rollback root remains
backups/atlas_platform/index_pre_commons_layers_20260717.html).
